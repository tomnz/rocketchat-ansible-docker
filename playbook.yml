---
- name: Configure all servers
  hosts: rocketchat
  become: true
  vars:
    pip_install_packages:
      - name: docker
    docker_package_state: latest
  roles:
    - pip
    - docker
  #  - rocketchat.docker
